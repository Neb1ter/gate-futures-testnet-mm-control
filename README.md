# Gate Futures Testnet Market Maker

Deployable Render bundle for the Gate Futures Testnet market-making service.

Main app files live in `paper_market_maker/`.

See `paper_market_maker/GATE_FUTURES_README.md` for local run, cloud env vars, and control API usage.
